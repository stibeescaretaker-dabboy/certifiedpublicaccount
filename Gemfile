source "https://rubygems.org"
# Lightweight local dev gems. GitHub Pages builds the site with its own
# github-pages gem, so this file is only needed for `jekyll serve` locally.
gem "jekyll", "~> 4.3"
