Read mode refinement: statement overlay gaps collapsed to a single blank line between paragraphs and after the title; poem duo now stacks into a readable single column in the phone overlay (a 375px column can't hold two poem columns without wrapping that broke the alignment). Desktop overlay and world views keep their side-by-side aligned layout.
