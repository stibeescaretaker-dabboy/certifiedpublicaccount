Fix phone overlay poem shrinking: the duo's font-size/line-height needed !important to beat the read-page nullifier (.read-page p/span {font-size:inherit!important}), which was forcing the duo to keep 19px and defeating the shrink. Now 15px/1.4 applies, columns fit side-by-side without wrapping, and the 84px offset lines columns up.
