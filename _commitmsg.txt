Overlay interaction fix: while the read/img overlay is open, world pan/zoom pauses (pointerdown and wheel handlers gated on an overlayOpen flag), and the overlay becomes drag-scrollable — mouse drags scroll the content, wheel scrolls natively, touch keeps native pan-y. No visual changes.
