Phone overlay poem: instead of stacking, shrink the duo to 15px/1.4 with a small gap so both columns fit side-by-side WITHOUT wrapping — the 4-line offset then pairs "you the pebble"<gap>"I the ripple" on one line all the way to "you sweat"<gap>"I the chemistry".
