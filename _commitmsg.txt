Reading-mode fixes: each "read" toggle now matches its section's text size and sits one full line lower; the reading overlay forces the cloned section back to the comfortable 22px/1.8 measure and strips all section padding, so text is no longer cut off or oversized.
