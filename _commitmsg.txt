Section 2 read mode: poem stanzas no longer overlap (in-world nudge margins with big negative values are neutralized at reading scale); the duo stays two side-by-side columns with the right one offset 4 lines so "I the ripple" lines up with "you the pebble" in the reading view too.
