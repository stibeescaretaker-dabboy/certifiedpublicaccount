Reading mode control restyled as a toggle (small square + "read" label, like the other controls), screen-fixed so it never moves while panning, scoped to section 1; checkbox opens/closes the black reading overlay.
