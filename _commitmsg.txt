Baro read mode round 2: title/IMG/LEAD lines are plain left-aligned blocks in the centered reading column (no more pseudo-center-aligned fit-content blocks), and the "&" is restored between the two titles.
