Fix reading mode: poem stanza spacing restored (toggle moved out of the space-between .poem-text into the section); overlay now allows touch-drag scrolling (pan-y); no "img" toggle in the statement section (no images).
