Phone read-mode fix: the statement text was invisible on phones because the world's .secstatement p{padding-left:480px} leaked into the read clone (only block/section/div padding was cleared, not p), shoving all prose off the right edge of a 390px screen. Now .read-page p clears padding too, keeping the statement in-column on both desktop and phone.
