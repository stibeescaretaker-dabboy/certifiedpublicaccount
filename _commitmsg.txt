Reading mode: each section now has its own small "read" toggle (styled exactly like the assist toggles) placed inside the section, so it travels with the world. Checking opens a black overlay with just that section's text re-flowed for comfortable reading.
