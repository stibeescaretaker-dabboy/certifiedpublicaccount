Locked zoom now starts OFF (checkbox unchecked by default)
Overlay scroll fixes: img-mode pointerdown no longer preventDefaults (canceling pointerdown was suppressing the native scroll that follows); img-mode wheel now only zooms on ctrl/pinch, plain wheel scrolls natively; mouse drag-scroll yields to the zoom pan while zoomed in
Virtual fist cursors sized to 32px tall (matches OS cursor render size; was 38px wide = ~61px tall)
Scroll fist spawns 100px away from the cursor on the side it's on (left half -> left, right half -> right)
Scroll fist: slower travel (0.5 -> 0.25 of scroll delta) and spawns 100px left of the cursor's position when scrolling began
Zoom-compensating halo capped at 2.5x (was enormous at max zoom-out); same halo added to all header toggles (movement assist, locked zoom, bio) — email span excluded
Toggle hit halo now compensates for zoom: panzoom exposes 1/current-zoom as --hs and the halo scales by it, staying a constant size on screen (buttons were hard to click when zoomed out)
read/img hit area: more generous (left 45px, right 25px, top/bottom 22px halo)
Scroll fist: back to one persistent fist per gesture (per-event cap reverted), moving opposite the scroll unclamped — when it runs off the top or bottom of the screen it resets to mid-screen and starts over
Pinch sensitivity up again (0.005 -> 0.012); scroll fist reworked: every scroll tick spawns its own fist offset opposite the scroll from mid-screen (marching trail, no clamp/cap) — the persistent stuck-looking fist is gone
Trackpad feel: pinch zoom 3x more sensitive (0.0016 -> 0.005 coefficient); pinch pair straddles the cursor diagonally at 45 degrees starting 270px apart; scroll fist spawns halfway up, tracks rapid scrolling (per-event movement capped at 40px so it never clamps)
Wheel fists fixes: pinch pair now straddles the cursor (one above, one below, cursor centered) starting 220px apart; scroll fist respawns fresh at the bottom on every new scroll gesture (the old one stayed stuck where it drifted)
Trackpad support: two-finger scroll pans the column (axis lock honored), trackpad pinch (ctrl+wheel) zooms; mouse wheel unchanged. Scroll spawns a fist in the bottom half moving opposite the scroll; pinch shows a fist at the focal point plus one starting 120px away that spreads/closes with zoom; both open and fade 2s after the gesture stops
Thin dividers 20px tall (was 27)
Thin section dividers 3/4 as tall (36px -> 27px)
read/img: square-to-label space (gap 14px -> 0.65em, scales with font), one size larger again (39/46/48/41 -> 43/51/53/45); momentum cap 34 -> 40
read/img labels one font size larger again (35/42/44/37 -> 39/46/48/41)
Momentum cap 26->34; img overlay: pinch/wheel zoom into images (1-8x), drag to pan while zoomed, zoom resets on open/close
Momentum: axis lock now governs the glide (no diagonal drift with movement assist on); pinch re-arm counter only counts single-finger movement AFTER the pinch — counting two-finger moves re-armed mid-pinch and rebuilt the slingshot
Slingshot fix: pinched stays frozen through the staggered finger-lift; velocity only re-arms after ~24px of genuine dragging with the remaining finger (or on a fresh gesture)
Fix: pinched flag never reset (dead condition in pointerdown) — now resets on new first finger and when a pinch ends with one finger still down, re-arming the glide
Momentum fixes: no glide after a pinch (pinched flag, velocity zeroed on 2nd finger — the cross-finger velocity was causing the acceleration/drift), glide weakened (decay 0.93->0.90, threshold 1.2->2, release speed capped at 26px/frame)
Fluidity: rAF-batch pan/pinch transform writes (phones fire more moves than frames); add inertial glide after drag release (decay 0.93, killed by any new touch/zoom); slow releases still stop in place
Poem last stanza (desktop) up 1 line: -57px margin-top
Poem stanza 1 (desktop) lowered 2 more lines (3->5): 171px->285px nudge, negative bottom keeps everything else in place
Zoom-out-on-click moved to UNTOGGLING locked zoom (leaving locked mode zooms out x0.85); toggling it on keeps the fit-scale pull-in
Clicking locked zoom now zooms out a little (x0.85, clamped to the fit scale)
read/img labels: red switched to purple (#a855f7)
Restore red for read/img labels only (they are the only elements .read-toggle styles); larger sizes unchanged
Undo red text on read/img toggles (keep the one-size-larger labels)
read/img toggles: one font size larger (32-40 -> 35-44) and red text, squares stay white
poem-piece.jpg re-encoded at 3600px q95 (~1.8MB) matching the other photos' sharpness
Re-encode all section photos at 2400px q90 (was 1600px q82) closer to original PNG quality
Temporarily hide the up/down section nav widget pending redesign - code kept, one CSS line to restore
Poem overlay gap: right column now ends at ~333px, right before the ~355px column
edge (left:10em), per request. Offset still exactly 4 lines, 14/5 lines, no wrapping.
